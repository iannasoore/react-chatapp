import './index.css';
// This file is kept for compatibility with CRA-style tests. The app is mounted
// from /src/main.jsx when using Vite (index.html loads main.jsx).
// Re-export the App component if tests import it.
import App from './main.jsx';
export default App;
